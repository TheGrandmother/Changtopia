module main

def bob()
  return 420
end

def _entry()
  a = 'hello'
  d = [<a>, "\n", a]
  bif:send(0, $print, d)
  bif:send(0, $print_raw, d)
  bif:send(0, $print_raw, bif:length(d))
  bif:send(0, $print_raw, d#(bif:length(d) - 1))
  bif:send(0, $print, d#(bif:length(d) - 1))
  return 0
end
