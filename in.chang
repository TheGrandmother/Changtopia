module main

def _entry()
  array_bro = [16, 5, 69,69,69,69,69,69, 7, 2]

  match array_bro
    [a, 5, <yz>, 9, x] ->
      bif:send(0, $print, 'Array with blob')
    end
    $otto_von_bissmark ->
      bif:send(0, $print, 'Matched atom')
    end
    whatever ->
      bif:send(0, $print, 'Fuckitall')
    end
  end

  bif:send(0, $print, 'Ended like a fokkin pimp')
  return 420
end
